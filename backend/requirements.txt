Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.1
django-filter>=23.5
celery>=5.3.6
redis>=5.0.1
psycopg2-binary>=2.9.9
python-dotenv>=1.0.1
PyJWT>=2.8.0
# Email delivery via SendGrid's official SDK (used by tracker.email_backends.SendGridBackend).
# Requires SENDGRID_API_KEY env var in production; falls back to Django's console backend otherwise.
sendgrid>=3.6.5,<7.0.0
# Solution scraper: fetch leetcode.ca solution pages server-side (never proxied to clients).
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
gunicorn>=21.2.0
flake8>=6.0.0
drf-spectacular>=0.27.0
sentry-sdk>=2.0.0
